{
  "$id": "jira-harness/harness.schema.json",
  "title": "jira-harness project config (.codex/harness.json)",
  "description": "프로젝트만 아는 값. 절대 경로·자격증명 금지(머신별 값은 stacks.<name>.env_file 이 가리키는 gitignore 파일로).",
  "type": "object",
  "required": [
    "version",
    "mode",
    "issue_prefix",
    "branch_pattern",
    "stacks"
  ],
  "properties": {
    "version": {
      "type": "integer",
      "minimum": 3,
      "maximum": 3
    },
    "mode": {
      "type": "string",
      "enum": [
        "auto",
        "suggest",
        "off"
      ],
      "description": "auto=차단 · suggest=경고만 · off=비활성"
    },
    "issue_prefix": {
      "type": "string",
      "pattern": "^[A-Z][A-Z0-9]+$"
    },
    "branch_pattern": {
      "type": "string",
      "description": "named group <keys> 가 이슈 번호 열을 캡처한다. 예: ^(feat|fix)/(?<keys>ABC-\\d+(?:-\\d+)*)(?:-[a-z0-9]+)*$"
    },
    "default_branch": {
      "type": "string",
      "description": "기본 main"
    },
    "default_branch_policy": {
      "type": "string",
      "enum": [
        "deny",
        "allow"
      ],
      "description": "default_branch 에서의 commit·push — deny(기본, 이슈 브랜치 강제) / allow(1인 저장소: 머지 결과를 default_branch 로 올린다). allow 면 그 브랜치에서는 게이트·리뷰 기록을 판정하지 않는다"
    },
    "shell": {
      "type": "string",
      "enum": [
        "auto",
        "bash",
        "sh",
        "cmd"
      ],
      "description": "게이트 명령을 돌릴 셸. auto = win32 면 bash(Git Bash) 우선, 없으면 cmd; 그 외 sh"
    },
    "runtime_dir": {
      "type": "string",
      "description": "기본 .codex/runtime"
    },
    "stacks": {
      "type": "object",
      "additionalProperties": {
        "$ref": "#/$defs/stack"
      }
    },
    "dispatch": {
      "type": "object",
      "description": "verify 레인 배정: 경로 glob → 에이전트 이름 목록(프로젝트 agents/ 또는 jira-harness:* 제네릭)",
      "additionalProperties": {
        "type": "array",
        "items": {
          "type": "string"
        }
      }
    },
    "review": {
      "type": "object",
      "properties": {
        "codex": {
          "type": "boolean"
        },
        "codex_via": { "type": "string", "enum": ["exec", "herdr"], "description": "Codex 리뷰 경로 — exec(기본, codex-review.sh 가 codex exec 를 매번 새로 띄운다) · herdr(Herdr 안이면 상주 codex pane 을 재사용 — herdr-lanes.mjs codex-review. Herdr 밖이면 exec 로 폴백)" },
        "codex_timeout": {
          "type": "integer",
          "minimum": 60
        },
        "lanes_max": {
          "type": "integer",
          "minimum": 1,
          "maximum": 8
        },
        "lanes_when": {
          "enum": ["codex_gap", "always", "never"],
          "description": "Claude 리뷰 레인을 언제 도는가 — codex_gap(기본): Codex 가 판정을 못 냈거나 Codex 가 볼 수 없는 축일 때만 · always: 매번 · never: 안 돈다"
        },
        "lane_model": {
          "type": "string"
        },
        "rounds_max": {
          "type": "integer",
          "minimum": 1
        },
        "code_review": {
          "type": "boolean"
        }
      }
    },
    "models": {
      "type": "object",
      "properties": {
        "orchestrate": {
          "type": "string"
        },
        "design": {
          "type": "string"
        },
        "recon": {
          "type": "string"
        },
        "implement": {
          "type": "string"
        },
        "verify": {
          "type": "string"
        }
      }
    },
    "wiki": {
      "type": "object",
      "properties": {
        "index": {
          "type": "string"
        },
        "log": {
          "type": "string"
        },
        "schema": {
          "type": "string"
        },
        "dev_guide": {
          "type": "string",
          "description": "{KEY} 자리표시자 포함 경로 템플릿"
        },
        "synthesis_dir": {
          "type": [
            "string",
            "null"
          ]
        },
        "max_pages_per_closure": {
          "type": "integer"
        },
        "claude_md_max_lines": {
          "type": "integer",
          "minimum": 20,
          "description": "issue-complete 가 CLAUDE.md 줄 수가 이 값을 넘으면 마감을 거부한다(closure 를 CLAUDE.md 에 쌓지 못하게). 기본 150"
        }
      }
    },
    "jira": {
      "type": "object",
      "properties": {
        "project": {
          "type": "string"
        },
        "start_transition": {
          "type": "string"
        },
        "done_transition": {
          "type": "string"
        },
        "comment_lang": {
          "type": "string"
        }
      }
    },
    "docs_only_paths": {
      "type": "array",
      "items": {
        "type": "string"
      }
    },
    "fingerprint_exclude": {
      "type": "array",
      "items": {
        "type": "string"
      }
    },
    "herdr": {
      "type": "object",
      "description": "Herdr(터미널 워크스페이스) 연동. Herdr 밖(HERDR_PANE_ID 없음)에서는 설정과 무관하게 아무것도 하지 않는다",
      "properties": {
        "enabled": { "type": "boolean", "description": "false 면 사이드바 토큰·알림·레인 실행기 전부 끈다. 기본 true" },
        "notify": { "type": "boolean", "description": "false 면 토스트 알림만 끈다(사이드바 토큰은 유지). 기본 true" },
        "lanes": { "type": "string", "enum": ["off", "verify", "all"], "description": "Herdr pane 을 레인 실행기로 쓰는 범위 — off(기본) · verify(리뷰 레인만) · all(예약)" },
        "kinds": {
          "type": "object",
          "description": "레인 종류별 에이전트 kind 목록(herdr agent start --kind). 예 verify: [\"codex\",\"grok\"]",
          "properties": { "verify": { "type": "array", "items": { "type": "string" } } }
        },
        "kind_args": {
          "type": "object",
          "description": "kind 별 네이티브 인자(`agent start … -- <args>`). 예 codex: [\"--sandbox\",\"danger-full-access\",\"--ask-for-approval\",\"never\"]",
          "additionalProperties": { "type": "array", "items": { "type": "string" } }
        },
        "lane_timeout_s": { "type": "integer", "minimum": 30, "description": "레인 1개의 응답 대기 상한(초). 기본 900" },
        "reviewer_context": { "type": "string", "enum": ["issue", "always", "never"], "description": "상주 reviewer 의 컨텍스트를 언제 비우나(/new) — issue(기본: 이슈 slug 가 바뀌면) · always(매 호출) · never" },
        "close_panes": { "type": "boolean", "description": "레인이 끝나면 pane 을 닫는다. 기본 false(사람이 화면을 본다)" }
      }
    },
    "gate": {
      "type": "object",
      "properties": {
        "timeout_s": {
          "type": "integer",
          "minimum": 30
        },
        "commit_budget_s": {
          "type": "integer",
          "minimum": 30,
          "description": "경량 게이트 목표 시간(초과해도 실패는 아니고 기록만)"
        }
      }
    },
    "branch_template": {
      "type": "string",
      "description": "issue-start 가 만드는 브랜치 이름. {keys} = 첫 키 전체 + 나머지 키 번호(같은 접두사일 때). 기본 feat/{keys}"
    }
  },
  "$defs": {
    "stack": {
      "type": "object",
      "required": [
        "dir"
      ],
      "properties": {
        "dir": {
          "type": "string",
          "description": "프로젝트 루트 기준 상대 경로. '.' 허용"
        },
        "paths": {
          "type": "array",
          "items": {
            "type": "string"
          },
          "description": "이 스택에 속하는 파일 glob. 생략 시 <dir>/**"
        },
        "compile": {
          "type": [
            "string",
            "null"
          ]
        },
        "lint": {
          "type": [
            "string",
            "null"
          ]
        },
        "build": {
          "type": [
            "string",
            "null"
          ]
        },
        "test": {
          "type": [
            "string",
            "null"
          ]
        },
        "extra": {
          "type": "array",
          "items": {
            "type": "string"
          }
        },
        "env_file": {
          "type": [
            "string",
            "null"
          ],
          "description": "KEY=VALUE 줄 파일(gitignore). 게이트 명령 환경에 주입"
        }
      }
    }
  }
}
